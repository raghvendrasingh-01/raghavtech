"""Service layer: PDF parsing, NLP scoring, and skill extraction.

Each module here is framework-agnostic (no FastAPI imports) so the logic can be
unit-tested or reused independently of the web layer.
"""
